<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
  {{style('bootstrap.min')}}
  {{style('font-awesome')}}
  {{style('style')}}
  {{style('clean-blog.min')}}
</head>
<body style="background:white">
  <link rel="stylesheet" type="text/css" href="style.css">


  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container">
      <a class="navbar-brand" href="#">First</a>
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
        Menu
        <i class="fas fa-bars"></i>
      </button>
      <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="/index">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/post">My Post</a>
          </li>
       
          <li class="nav-item dropdown show">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-Toggle="dropdown" aria-haspopup="true" aria-expanded="true" href="/logout">Logout
              
             </a>

          </li>
        </ul>
      </div>
    </div>
  </nav>


@!include('inc/navbar')
  
  <header class="masthead" style="background-image: url('img/blogg.png')">
    <div class="overlay homeLoad"></div>
    <div class="container">
      <div class="row">
       <div class="col-lg-4 col-md-6 mx-auto">
         
          <div class="post-heading post-title">
            <h1>Hello there</h1>
            <span class="subheading">"You can make anything  by writing"</span>
          </div>
        </div>
      </div>
    </div>
  </header>
  
  <article>
    <div class="container">
      <div class="row">
        <div class="col-lg-9 col-md-10 mx-auto">
          <h2>{{title}}</h2><p style="margin:0;"><a href="{{route('/myposts/add')}}"><a href="#" class="btn btn-primary btn-xs">Create Post</a>
          <p>
          @each(post in posts)
           <a href = "/posts/{{ post.id }}">
          <div class="blogpost">
          <div class="titles">
         {{post.title}}</div>
          <div class="data">{{post.desc}}
          <p style="margin-top: 10px; margin-bottom: 10px; text-align: right">{{post.created_at}}|by: {{post.userinfo.username}}
          </div>
          </div>
          </a>
          @endeach
      </div>
    </div>
  </div></article><br><br>
</body>
<script src="{{ assetsUrl('jquery.js') }}">
<script src="{{ assetsUrl('bootstrap.min.js') }}">
<script src="{{ assetsUrl('clean-blog.js') }}">
</html>